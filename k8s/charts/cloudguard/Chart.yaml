apiVersion: v2                 # Helm chart schema version (v2 for Helm 3)
name: cloudguard               # Chart name (used in release naming)
description: CloudGuard application Helm chart
type: application              # "application" vs "library"
version: 0.1.0                 # Chart (package) version
appVersion: "1.0.3"            # Your app's version (informational)

# Pstgresql section
dependencies:
  - name: postgresql
    version: 13.2.5
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
    alias: postgresql
